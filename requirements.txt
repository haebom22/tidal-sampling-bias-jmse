earthengine-api>=0.1.400
geemap>=0.32.0
pyfes>=2024.1   # CNES/AVISO engine for FES2022b tide synthesis (see src/tides/fes2014.py)
numpy>=1.26
pandas>=2.2
pyarrow>=15.0
# fastparquet is used as an automatic fallback for to_parquet() because
# pyarrow ≥24 + rasterio currently clash on the LocalFileSystem factory.
fastparquet>=2024.2
geopandas>=0.14
shapely>=2.0
xarray>=2024.1
netCDF4>=1.6
rasterio>=1.3
scipy>=1.12
matplotlib>=3.8
seaborn>=0.13
PyYAML>=6.0
tqdm>=4.66
requests>=2.31
python-dateutil>=2.8
jupyter>=1.0
ipykernel>=6.29
sliderule>=5.4
scikit-learn>=1.4
# Optional: eo-tides for native pixel_stats (preferred over our fallback).
# eo-tides>=0.5
